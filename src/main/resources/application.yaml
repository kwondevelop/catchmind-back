server:
  port: 8085

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/catchmind
    username: root
    password: msa505
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true